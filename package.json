{
  "name": "opencode-cowork-proxy",
  "version": "2.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "build": "esbuild src/server.ts --bundle --outfile=dist/index.js --format=esm --platform=node",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260504.1",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.87.0"
  },
  "license": "MIT",
  "description": "Bidirectional AI API translation gateway for Cloudflare Workers. Anthropic↔OpenAI format bridge.",
  "dependencies": {
    "@hono/node-server": "^2.0.4",
    "hono": "^4.12.17"
  }
}
